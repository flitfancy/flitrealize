{
  "name": "view-state",
  "private": true,
  "description": "Read-only ViewState paragraphs from the project handoff",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "cli": "node cli.mjs"
  }
}
