# 3.4 PCB 布局与空间

比较功能块布局、检查空白区域或移动明确器件时读取。只处理本页支持的几何操作；布局依据见 [3.1 PCB 设计](../../3.1-pcb-review.md)。板框创建另见 [3.2 PCB 基础](3.2-pcb-foundation.md)。

## 调用、回读与保存

所有 EDA 请求必须给出现场确认的 `expectedProjectUuid`、`expectedDocumentUuid`；坐标和线宽单位为 mil，不猜测 mm，不复制其他项目的目标或参数。现场连接、写入与恢复边界见 [Provider 入口](../../0.3-easyeda-pro.md)。

```text
node <skill>/scripts/pcb-edit.mjs --action pcb-placement --project-root <当前项目> --window-id <窗口> --input-file <plan-input.json>
node <skill>/scripts/pcb-edit.mjs --action pcb-placement --project-root <当前项目> --window-id <窗口> --input-file <plan-input.json> --candidate <唯一候选名> --apply
```

使用 [pcb-edit.mjs](../../../scripts/pcb-edit.mjs) 和下文的 plan 输入，不另造参数格式。默认只读 EDA，但会把计划、输入、源码备份和报告写入本项目 `evidence/pcb-edit/<唯一运行目录>/`。先检查候选 issues 和证据；多候选写入须明确唯一候选名，冲突方案不能执行；普通单方案不填 `--candidate`。

`--apply` 衔接 plan → apply → verify → save → 保存后 verify，逐步保留证据，任何失败即停。需要 inspect 或单独一步时使用 `action-runner.mjs run --action pcb-placement`，沿返回的 `applyRequest`、`verifyRequest`、`saveRequest` 传递，写入加 `--allow-write`；普通完整流程无需手工拆装这些请求。

## 布局和空白检查

先固定机械接口与关键功率/敏感功能块，再把允许移动的小块作为候选移向空白区。布局脚本不替你决定电源环路、电气隔离或哪个块重要。

示例只展示字段，不是任何实际电路的推荐尺寸：

```json
{
  "mode": "plan",
  "expectedProjectUuid": "<实际项目 UUID>",
  "expectedDocumentUuid": "<实际 PCB UUID>",
  "boardBounds": {"minX": 0, "minY": 0, "maxX": 2000, "maxY": 1200},
  "lockedDesignators": ["J1", "U1", "L1"],
  "reservedRegions": [{"name": "display", "bbox": {"minX": 1400, "minY": 800, "maxX": 1900, "maxY": 1100}}],
  "clearanceMil": 5,
  "cellMil": 50,
  "scenarios": [
    {"name": "move-control-left", "groups": [{"designators": ["U3", "C8", "R9"], "dxMil": -100, "dyMil": 0}]},
    {"name": "move-control-down", "groups": [{"designators": ["U3", "C8", "R9"], "dxMil": 0, "dyMil": 100}]}
  ]
}
```

- 单方案不用 `scenarios`，直接给 `groups` 或 `placements: [{designator, x, y, rotation?}]`。未给 rotation 时保留当前旋转，不默认归零；组内只平移，保持相对关系。旋转仅支持相对当前角度的 90° 整数倍。
- `lockedDesignators` 和 `reservedRegions` 必须显式给出，确认没有约束时才用空数组。EDA 自身已锁定器件也不能移动；先核对锁定对象，不通过脚本解除锁。
- 现场所有 Region 的包围盒也作为保守障碍，可能比实际禁布范围大。尚未体现在 Region 中的屏幕、装配、天线、散热预留必须通过输入提供。
- `inspect` 只给目标即可读取完整器件快照；需要空白分析时再给上述边界/约束，不需给移动项。
- 最大空白矩形仅基于网格中的器件/Region 包围盒，不代表可压缩面积或可用布线面积；网格越粗越保守。对侧器件也保守计入，不自动假设可重叠。
- 仅支持明确矩形边界，不处理异形轮廓、孔槽或复杂三维干涉；这些条件不满足时改用真实几何检查，不伪造矩形验收。
- 每批最多 100 个器件、最多 20 个候选、网格最多 100000 格。先比较少量有依据的候选，不枚举任意坐标。
- **存在铜层线路、圆弧、折线、过孔或铺铜时，不执行器件移动**，因为本入口不会带动导线或自动清线。已布线板先按用户意图单独处理受影响走线，或使用适合已布线调整的工具。当前源备份不能替代这项约束。

plan 返回每个候选的 `issues`、`changedCount`、`space` 和可用的 applyRequest。apply 回读目标及未移动器件的已读属性；包围盒无冲突不等于焊盘间距、连通性、热设计或整板 DRC 合格。

## 结果与失败恢复

`result.json` 汇总本次结果并引用分步证据；将实际检查范围和稳定结论同步到同一份 `CURRENT_HANDOFF.md`。只读 verify 不查询编辑器保存状态，返回 `saved: null, saveChecked: false`；结合独立 save 报告和保存后的现场指纹判断，不把 verify 当作保存或整板验收。

applyRequest 绑定当前源码和已读对象指纹；用户修改、换板或目标错误会被拒绝。调用期间保持单一写入者；本入口没有跨进程事务锁，也没有原理图批量入口的自动续接机制。

保存得到确定失败结果后，使用本项目成功 apply 报告或 result.json 续存：

```text
node <skill>/scripts/pcb-edit.mjs --project-root <当前项目> --window-id <窗口> --resume-save <本项目成功apply报告或result.json> --apply
```

入口核对项目归属、目标、请求及当前指纹，再 verify → save → verify，绝不重放 apply。现场已变化、保存结果未知或仍在执行时，先对账，不强行续存。

同一成功 apply 的保存尝试会在 `evidence/pcb-edit/save-attempts/` 留下占用记录，防止通过 result、原报告或副本重复发起未决保存。确定终态后释放，未知或进程中断时保留；不要删记录绕过，先确认旧执行结束并核对现场。此记录只保护该保存链，不证明其他 EDA 写入者不存在，也不是新交接文档。

写入失败返回 `apply-failed`、写入前快照、已尝试 ID 及能取得的当前回读，不自动回滚或保存。超时且没有确定终态时，先确认旧执行停止并核对现场，再计划受影响增量；未收到成功不等于未写入。源码备份不应整图导入覆盖后续用户修改。

布局移动使用 [PCB_PrimitiveComponent.modify](https://prodocs.lceda.cn/cn/api/reference/pro-api.pcb_primitivecomponent.modify.html)，读取原生 PrimitiveLock。方法存在不证明当前版本行为，仍执行回读验证。

## 同步项目交接

在主文稿“布局与空间”主题同步本次涉及的功能块、成员位号、固定与可移动条件、预留区、布局理由、实际回读及保存范围；未检查的电气关系和后续布线明确保留。只更新对应正文、顶部当前交接和该主题的 `ViewState:` 梗概。规则见 [0.1 项目主文稿](../../0.1-continuation.md)。
