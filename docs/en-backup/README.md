# 英文历史备份

此目录保存 2026-09-11 切换中文执行源之前的英文说明，文件使用 `.bak` 后缀，不参与 Skill 执行或运行包。当前执行规则只维护在根目录 `SKILL.md`、`references/` 和 `development/` 中。

`manifest.json` 保存原路径和备份文件的 SHA-256。校验只检查备份完整性，不要求英文跟随中文更新，也不通过刷新哈希掩盖备份变化。需要回看旧规则时才读取；历史内容不能覆盖当前中文规则。

这是说明文档快照，不是完整仓库备份。完整恢复应使用本次修改前单独保存、包含未提交文件和 Git 元数据的全量备份。
